name: CI-C

on:
  workflow_dispatch:
  
  push:
    branches: 
      - "main"
  
jobs:
  
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Setup Cpp (C++ / C)
        uses: aminya/setup-cpp@v0.26.2
        with:
          compiler: gcc
          make: true
          cache: cppcache
      - name: Set up NASM
        uses: ilammy/setup-nasm@v1.4.0


      - name: build main
        run: make main --directory=C

      - name: build project
        run: make --directory=C

      - run: sudo ./C/output/exemple1
        continue-on-error: true
      - run: sudo ./C/output/exemple2
        continue-on-error: true
      - run: sudo ./C/output/exemple3
        continue-on-error: true
      - run: sudo ./C/output/exemple4
        continue-on-error: true
      - run: sudo ./C/output/exemple5
        continue-on-error: true

